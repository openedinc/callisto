<p id="notice"><%= notice %></p>

<h1>Assessment Item Events</h1>

<table>
  <thead>
    <tr>
      <th>Actorid</th>
      <th>Action</th>
      <th>Objectid</th>
      <th>Maxscore</th>
      <th>Ispartof</th>
      <th>Generatedid</th>
      <th>Generatedcount</th>
      <th>Generatedstartedattime</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @assessment_item_events.each do |assessment_item_event| %>
      <tr>
        <td><%= assessment_item_event.actorId %></td>
        <td><%= assessment_item_event.action %></td>
        <td><%= assessment_item_event.objectId %></td>
        <td><%= assessment_item_event.maxScore %></td>
        <td><%= assessment_item_event.isPartOf %></td>
        <td><%= assessment_item_event.generatedId %></td>
        <td><%= assessment_item_event.generatedCount %></td>
        <td><%= assessment_item_event.generatedStartedAtTime %></td>
        <td><%= link_to 'Show', assessment_item_event %></td>
        <td><%= link_to 'Edit', edit_assessment_item_event_path(assessment_item_event) %></td>
        <td><%= link_to 'Destroy', assessment_item_event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Assessment Item Event', new_assessment_item_event_path %>
