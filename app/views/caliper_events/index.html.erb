<p id="notice"><%= notice %></p>

<h1>Caliper Events</h1>

<table border="1">
  <thead>
    <tr>
      <th>Payload</th>
      <th>Created At</th>
      <th>Delete</th>
    </tr>
  </thead>

  <tbody>
    <% @caliper_events.each do |caliper_event| %>
      <tr>
        <td><%=link_to "View",@action%> <%= link_to 'Edit', edit_caliper_event_path(caliper_event) %></td>
        <td><%= caliper_event.created_at %></td>
        <td><%= link_to 'X', caliper_event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
